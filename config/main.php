<?php
if( !defined("ROOT_DIR") ){
    define( "ROOT_DIR", realpath( dirname(__FILE__) . '/../' ) );
}
if( !defined("PUBLIC_DIR") ){
    define( "PUBLIC_DIR", ROOT_DIR . '/contact' );
}
if( !defined("STYLE_DIR") ){
    define( "STYLE_DIR", ROOT_DIR . '/public/css/stylesheets' );
}
if( !defined("IMG_DIR") ){
    define( "IMG_DIR", ROOT_DIR . '/public/img' );
}
if( !defined("TPL_DIR") ){
    define( "TPL_DIR", ROOT_DIR . '/public/template' );
}
if( !defined("ENGINE_DIR") ){
    define( "ENGINE_DIR", ROOT_DIR . '/engine' );
}
if( !defined("CONFIG_DIR") ){
    define( "CONFIG_DIR", ROOT_DIR . '/config' );
    // echo CONFIG_DIR;
}
if( !defined("TPL_DIR") ){
    define( "TPL_DIR", ROOT_DIR . '/template' );
}